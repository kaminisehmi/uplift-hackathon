[
  {
    "id": "BC-001",
    "title": "BaseSettings moved to pydantic-settings",
    "description": "`from pydantic import BaseSettings` raises `PydanticImportError` in v2. Settings management now lives in the `pydantic-settings` package:",
    "detection_hint": "from pydantic import BaseSettings",
    "old_pattern": "# v1\nfrom pydantic import BaseSettings\n\n# v2\nfrom pydantic_settings import BaseSettings, SettingsConfigDict",
    "new_pattern": "",
    "confidence_required": 0.95
  },
  {
    "id": "BC-002",
    "title": "Validators renamed (@validator / @root_validator)",
    "description": "- `@validator(\"field\")` \u2192 `@field_validator(\"field\")` (import `field_validator`). Validators must be classmethods; add `@classmethod` under the decorator. - `@root_validator` \u2192 `@model_validator(mode=\"before\")` or `@model_validator(mode=\"after\")`. The bare v1 form `@root_validator` (post validation, no arguments) raises an error at class-definition time in v2. - `mode=\"after\"` validators receive and return the model instance (`self`), not a `values` dict. - `mode=\"before\"` validators receive the raw input data.",
    "detection_hint": "@validator|@root_validator",
    "old_pattern": "",
    "new_pattern": "",
    "confidence_required": 0.95
  },
  {
    "id": "BC-003",
    "title": "class Config \u2192 model_config / ConfigDict",
    "description": "The inner `class Config` is replaced by a `model_config = ConfigDict(...)` class attribute (import `ConfigDict` from `pydantic`). Renamed options:",
    "detection_hint": "class Config:",
    "old_pattern": "",
    "new_pattern": "",
    "confidence_required": 0.95
  },
  {
    "id": "BC-004",
    "title": "Field keyword renames (regex\u2192pattern, min_items\u2192min_length)",
    "description": "Removed keyword arguments raise `PydanticUserError` at class-definition time:",
    "detection_hint": "regex=|min_items=|max_items=|const=",
    "old_pattern": "",
    "new_pattern": "",
    "confidence_required": 0.95
  },
  {
    "id": "BC-005",
    "title": "Renamed model methods (.dict, .json, .copy, .schema, .parse_obj, .parse_raw)",
    "description": "The v1 methods still exist in v2 but emit `PydanticDeprecatedSince20` warnings and will be removed; migrate to the new names:",
    "detection_hint": "\\.(dict|json|copy|schema|parse_obj|parse_raw)\\(",
    "old_pattern": "",
    "new_pattern": "",
    "confidence_required": 0.95
  },
  {
    "id": "BC-006",
    "title": "Behavioral changes (frozen raises ValidationError; Optional without default)",
    "description": "- v2 is stricter about type coercion in some cases (e.g. `str` fields no longer accept arbitrary types); \"smart\" union matching replaced left-to-right union coercion. - `Optional[X]` fields without a default are required in v2 (in v1 they implicitly defaulted to `None`). Fields declared `Optional[X] = None` behave the same. - `.json()`/`model_dump_json()` output may differ slightly in whitespace and datetime formatting. - Error messages and `ValidationError` structures changed; tests that assert exact error text may need updating. `ValidationError` still raises on invalid input.",
    "detection_hint": "pytest\\.raises\\(TypeError\\)|TypeError",
    "old_pattern": "",
    "new_pattern": "",
    "confidence_required": 0.7
  }
]